// challenge.options = challenge.options.filter(
// 				(_: any, i: number) => i !== optionIndex
// 			)
import AdminPageShell from '../admin-page'

export default function GenerateChallengesRoute() {
	return <AdminPageShell />
}
