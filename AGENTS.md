# Project Agent Notes

## Learning Admin Actions

- Put learning-admin page actions in a consistent footer area at the bottom of the page, outside the card/form content.
- For read/detail pages, render actions in this order: Back, Update, Delete.
- Keep delete actions as icon-only trash buttons with accessible labels and screen-reader text.
- Prefer the shared learning action component instead of duplicating Back/Update/Delete markup.
